# 机制体验包验证记录

> 以下是历史六关版本的验证记录。当前 20 关版本的范围和证据见 [campaign-20.md](campaign-20.md)，不能把下述旧浏览器观察当作新关卡的验收。

## 第 0 关与横向选关补充验证

- 新增「初染」：复用原 6×6 切角布局，32 个彩格，最短 3 步；原 1—5 关题面、编号、链接不变。
- `npm test` 全通过，新增覆盖：六关 × 三尺寸，手势结束后点击、滑动不切关、取消/纵向手势、滚动边界、滚轮仅作用于关卡条、24 关列表不压缩按钮，以及初染通关进入聚流。
- 真实浏览器完成左右拖动、滚至先声并点击、再返回初染。拖动中 URL 与棋盘不变，轻点后才切关。
- 实际点完初染三步，看到「初染成画 / 3 步完成」弹框，点击「去试试『聚流』」进入第 1 关。
- 初次浏览器检查发现旧平台脚本缓存导致滑动接口缺失。给预览脚本统一版本标记后核对 `onTouchMove/onTouchEnd` 已加载，再次实际拖动通过。
- 视觉检查沿用 game-visual-qa 的可读性原则：选关按钮固定 74×44，不随关卡数量增加而挤小，超出部分裁切并显示滚动位置条。手机原生手势、真机安全区及留存提升尚未验证。
- STATUS: DONE_WITH_CONCERNS。Next Step: /game-qa，补真机横滑手感验证，用户先体验入门到挑战的难度衔接。

日期：2026-09-06。范围：五关试玩页与传色交互，原生 Canvas 2D，WIP 原型，不做最终美术评分。

## 自动化证据

- `npm test` 全部通过，含原三关回归、新五关、独立 BFS 对照、音效/胜利特效与双平台构建。
- 五关 × 390×844 / 375×667 / 320×568：模拟 Canvas 绘制、点击染色、动画输入锁、通关、胜利后撤销及重开通过。
- 最短步数 4/5/5/5/5，可行开局 1/3/1/1/4。传色关取消传色边后五步无解，铺垫关穷举固定起点五步无解。

## 实际浏览器观察

- 默认 736×702 浏览器内的 390 宽画布：检查了五关页面，选关能更新题面和 URL。
- 375×667：检查第五关和 10×10 第三关，规则、四个颜料按钮及提示按钮均可见，没有被底部裁切。临时尺寸覆盖已恢复。
- 第四关完成五步实际点击：第三步后两枚标记同蓝；第四步从远端落笔，两端都变黄；第五步统一红色并出现五步通关弹框。
- 按 game-visual-qa 的可读性检查缩小了颜料选中环，避免和按钮下方颜色名称相碰；规则放入独立两行，不与提示按钮抢空间。

## 关注项与未验证范围

- 375×667 上第三关单格约 25 像素，同色区域的总点击范围通常更大，但小色块的手指点击准确率未测。属于小屏触控风险，不当作视觉完成验收。
- 真机安全区、微信与抖音原生运行、真机帧率和音频听感，本次未验证。
- 通关截图捕获的是稳定弹框，不是瞬时彩带视觉证据。沿用的彩带模块仅做自动回归。
- 游戏视觉检查的 gstack 辅助程序在项目内不存在，采用本地测试和真实浏览器检查；不产生自动美术评分。
- 难度、传色点是否直观、第五关是否有恰当的顿悟感，等待用户体验。

Visual QA:
  Scope: 五关 UI 与传色交互，2D 原型
  STATUS: DONE_WITH_CONCERNS
  Visual Score: 未评分，非完整美术审查

Next Step:
  PRIMARY: /game-qa，结合用户试玩反馈及真机小屏点击验证，决定是否扩展到二十关。
